formatVersion: "1.0"
kind: requestDefinition
id: "payloads/multipart"
request:
  method: POST
  baseUrl: "{{apiAlias}}"
  path: "/api/multipart"
  multipart:
    note: "{{pipelineName}}"
    document:
      file: "fixtures/payload.txt"

